                #allows for random choices to be called from inside a list
import random
                #the list which will be randomised
flower_variants = [ 
    'rose', 'dandelion', 'buttercup', 'daisy', 'foxglove'
    ]
